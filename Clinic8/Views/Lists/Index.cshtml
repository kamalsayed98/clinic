@model IEnumerable<Clinic8.Models.List>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.list_dr_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.list_pat_id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.list_dr_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.list_pat_id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.list_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.list_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.list_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
