@model IEnumerable<Clinic8.Models.Reports>

@using Microsoft.EntityFrameworkCore
@using Clinic8.Data
@using Microsoft.AspNetCore.Identity;

@inject ApplicationDbContext _context

<<<<<<< HEAD
=======

>>>>>>> f997cdbcc9ce54b2f83b3db478c5039da256aa4f
<table class="table">
    <thead>
        <tr>
            <th>
                patient
            </th>
            <th>
                IC
            </th>
            <th>
                doctor
            </th>
            <th>
                title
            </th>
            <th>
                cost
            </th>
            <th>
                @Html.DisplayNameFor(model => model.rep_date)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @{
                        var patient = _context.Patient.Where(e => e.Id == item.rep_pat_id).Single();

                        var x = patient.pat_firstname + " "  + patient.pat_lastname;
                    }
                    @x
                </td>
                <td>
                    @{
                        var ins = _context.InsuranceCompany.Where(e => e.Id == item.ins_ref).Single();

                        var y = ins.ins_name;
                    }
                    @y
                </td>
                <td>
                    @{
                        var doc = _context.Doctor.Where(e => e.Id == item.rep_dr_id).Single();

                        var z = doc.dr_firstname + " " + doc.dr_lastname;
                    }
                    @z
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.rep_cons_title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.rep_cons_cost)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.rep_date)
                </td>
                
            </tr>
        }
    </tbody>
</table>
